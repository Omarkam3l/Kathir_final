import 'package:flutter/material.dart';
import 'restaurant_home_screen.dart';

/// Restaurant Dashboard screen - shows home screen
/// This is the main entry point for restaurant users
class RestaurantDashboardScreen extends StatelessWidget {
  const RestaurantDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const RestaurantHomeScreen();
  }
}
